[project]
name = "pedersen-detector"
authors = ["OpenAI Assistant"]
description = "A novel blockchain solution for detecting and tracking data distribution patterns"
telemetry = false
requirements = []
[contracts.pedersen-detector]
path = "contracts/pedersen-detector.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
